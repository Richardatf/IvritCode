# Security

The deterministic VM exposes no filesystem, network, shell, eval, or host-operation opcode. Browser code contains no secret. Chavruta sanitizes errors and validates requests and responses. Production uses configurable origins and security headers. Report vulnerabilities privately to the repository maintainers.
