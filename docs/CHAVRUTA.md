# Chavruta

IvritCode Chavruta is an optional programming and study partner. It uses a server-side OpenAI key, configured model, JSON schema, prompt limit, timeout, origin allow-list, and rate-limiting hook. Generated source is parsed before validity is claimed. Failures do not disable local computation.
