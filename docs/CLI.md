# CLI

The `ivritcode` CLI provides `run`, `check`, `trace`, `disassemble`, `gates`, `lexicon`, `convert`, `repl`, and `info`. Trace supports text, JSON, and NDJSON. Errors use nonzero exit codes and preserve UTF-8 source locations. Run `npm run cli -- <command>` from a checkout.
