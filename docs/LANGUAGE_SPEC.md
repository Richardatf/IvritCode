# Language specification

IvritCode 1.0 source is UTF-8 Hebrew text. The 22 canonical letters are operators; whitespace and ordinary punctuation separate them; `#` comments continue to line end. Execution follows logical Unicode order. `.ivc` is the standard extension. See `INSTRUCTION_SET.md` for semantics and `UNICODE.md` for normalization.
