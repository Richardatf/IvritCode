# Instruction set

The authoritative instruction registry is `packages/core/src/instructions.ts`. It defines all 22 technical transformations, names, indices, and Aleph Olam access. All reads for an operation come from its original input state. `תּ` performs Tav and explicitly halts; plain Tav records a seal checkpoint and continues.
