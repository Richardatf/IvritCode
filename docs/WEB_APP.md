# IvritCode Observatory

The React Observatory consumes workspace packages and contains no VM semantics. It provides source playback, register ring/grid/table views, restorable trace checkpoints, gate matrix/list views, lexicon categories, themes, uploads/downloads, and optional Chavruta. RTL is a presentation property only.
